import React from "react";


function Avatar (props2)
{
    return(
        <img className="circle-img"
        src={props2.imgUrl}
        alt="avatar_img"
      />

    //   this attempt will through u bugs and lots of confusion so we use devtools for more clear cut view
    // to use devtools go to developer tools and click components frm top
    )
}

export default Avatar;